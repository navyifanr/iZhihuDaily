<?xml version="1.0" encoding="utf-8"?>
<android.support.v7.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:foreground="?android:attr/selectableItemBackground"
    android:layout_marginLeft="@dimen/spacing_small"
    android:layout_marginRight="@dimen/spacing_small"
    android:layout_marginTop="@dimen/spacing_tiny"
    android:layout_marginBottom="@dimen/spacing_tiny"
    android:clickable="true"
    app:cardCornerRadius="3dp"
    app:cardElevation="8dp">

    <RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_margin="@dimen/spacing_small"
        >

        <ImageView
            android:id="@+id/iv_home_news_img"
            android:layout_width="75dp"
            android:layout_height="80dp"
            android:layout_centerVertical="true"
            android:layout_alignParentRight="true"
            tools:src="@mipmap/ic_launcher"
            />

        <TextView
            android:id="@+id/tv_home_news_title"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_toLeftOf="@id/iv_home_news_img"
            android:layout_marginRight="@dimen/spacing_tiny"
            android:layout_centerVertical="true"
            android:lineSpacingExtra="@dimen/spacing_tiny"
            android:textSize="@dimen/font_size_big_x"
            android:textColor="@color/black"
            android:maxLines="3"
            android:minLines="2"
            android:ellipsize="end"
            tools:text="this is a test, this is a test, this is a test, this is a test, this is a test, this is a test,this is a test, this is a test, this is a test, this is a test, this is a test,  "
            />

    </RelativeLayout>
</android.support.v7.widget.CardView>